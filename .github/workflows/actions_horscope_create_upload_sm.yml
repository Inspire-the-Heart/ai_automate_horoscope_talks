name: Horoscope Main Content

on:
  schedule:
    - cron: "0 23 * * *"   # 4:30 AM IST
  workflow_dispatch:

concurrency:
  group: vionix_pipeline_main
  cancel-in-progress: false

env:
  PYTHONUNBUFFERED: "1"
  CUBLAS_WORKSPACE_CONFIG: ":4096:8"

  GEMENI_KEY: ${{ secrets.GEMENI_KEY }}
  GEMENI_MODEL: ${{ secrets.GEMENI_MODEL }}
  LANGUAGE: ${{ secrets.LANGUAGE }}

  FB_VERSION: ${{ secrets.FB_VERSION }}
  FB_PAGE_ID: ${{ secrets.FB_PAGE_ID }}
  FB_PAGE_TOKEN: ${{ secrets.FB_PAGE_TOKEN }}

  INSTA_PAGE_ID: ${{ secrets.INSTA_PAGE_ID }}
  INSTA_PAGE_TOKEN: ${{ secrets.INSTA_PAGE_TOKEN }}

  JSON_CLIENT_YT: ${{ secrets.JSON_CLIENT_YT }}
  JSON_OAUTH_YT: ${{ secrets.JSON_OAUTH_YT }}

  HF_TOKEN: ${{ secrets.HF_TOKEN }}
  VIONIX_GIST_ID: ${{ secrets.VIONIX_GIST_ID }}
  OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}

  VIONIX_WHEEL_DIR: .vionix-wheel

jobs:
  run:
    name: 🚀 Generate Horoscope JSON files
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-python@v6
        with:
          python-version: "3.11"

      - name: Cache pip
        uses: actions/cache@v5
        with:
          path: ~/.cache/pip
          key: pip-${{ runner.os }}-py311-${{ hashFiles('**/requirements.txt') }}
          restore-keys: |
            pip-${{ runner.os }}-py311-

      - name: Cache Vionix wheel
        uses: actions/cache@v5
        with:
          path: ${{ env.VIONIX_WHEEL_DIR }}
          key: vionix-${{ runner.os }}-py311-${{ secrets.RELEASE_TAG }}

      - name: Install build tools
        run: |
          python -m pip install --upgrade pip build

      - name: Build Vionix wheel (if needed)
        env:
          GH_PAT: ${{ secrets.GH_TOKEN }}
        run: |
          mkdir -p $VIONIX_WHEEL_DIR
          if ! ls $VIONIX_WHEEL_DIR/*.whl 1>/dev/null 2>&1; then
            echo "🔨 Building Vionix from commit/tag: ${{ secrets.RELEASE_TAG }}"
            pip wheel \
              git+https://x-access-token:${GH_PAT}@github.com/simplycodesmart/Vionix.git@${{ secrets.RELEASE_TAG }} \
              -w $VIONIX_WHEEL_DIR
          else
            echo "✅ Using cached Vionix wheel"
          fi

      - name: Install dependencies
        run: |
          pip install $VIONIX_WHEEL_DIR/*.whl
          pip install -r requirements.txt --prefer-binary

      - name: Install FFmpeg
        uses: FedericoCarboni/setup-ffmpeg@v3
        with:
          ffmpeg-version: "6.1.0"

      - name: Create auth files
        run: |
          python - <<'EOF'
          import json, os
          json.dump(json.loads(os.environ["JSON_CLIENT_YT"]), open("client_secrets.json","w"), indent=2)
          json.dump(json.loads(os.environ["JSON_OAUTH_YT"]), open("oauth.json","w"), indent=2)
          print("✅ Auth files created")
          EOF

      - name: Execute Horoscope JSON Generate
        run: |
          python -c "import Vionix; print('✅ Vionix import OK')"
          python main_hrscp_content_create.py

      - name: Check generated files
        id: check_files
        run: |
          if ls horoscope_*.json 1> /dev/null 2>&1; then
            echo "has_files=true" >> $GITHUB_OUTPUT
          else
            echo "has_files=false" >> $GITHUB_OUTPUT
          fi

      - name: Upload horoscope language files
        if: steps.check_files.outputs.has_files == 'true'
        uses: actions/upload-artifact@v4
        with:
          name: horoscope-files
          path: |
            horoscope_te.json
            horoscope_ta.json
            horoscope_kn.json
            horoscope_ml.json
            horoscope_bn.json
            horoscope_mr.json
            horoscope_hi.json
          retention-days: 1

      - name: Upload generated videos
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: daily-horoscope-videos
          path: daily_horoscope_*.mp4
          retention-days: 1